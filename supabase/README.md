# Supabase folder

This folder is for Supabase-related artifacts:

- `migrations/` - SQL migrations
- `functions/` - Edge functions

Place schema and migration files under `migrations/` and serverless functions under `functions/`.
